name: scaped
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  flutter_dotenv: ^5.1.0
  flutter_localizations:
    sdk: flutter
  flutter_modular: ^5.0.3
  intl: any
  shared_preferences: ^2.2.0
  supabase_flutter: ^1.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets: 
    - lib/assets/logo.png
    - lib/assets/logo_mono.png
    - lib/assets/background.png
    - .env.dev
